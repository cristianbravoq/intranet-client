export interface ISendGiftCard {
  nombre: string;
  telefono: string;
  correo: string;
  monto: string;
  referencia: string;
}

export interface IDatosPersonales {
  co: string;
  fecha: string;
  monto: string;
  referencia: string;
}
